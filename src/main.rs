use bigint::*;

fn main() {
    // let u = BigInt::from(&[0x00, 0x01]);
    // u.print();
    // let v = BigInt::from(&[0x01, 0x00]);
    // v.print();

    // let w = BigInt::from(&[0xff, 0xff]);
    // w.print();
}
